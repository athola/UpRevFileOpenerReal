name: Android Build

on:
  push:
    branches: [ main, master, develop ]
  pull_request:
    branches: [ main, master, develop ]
  workflow_dispatch:

jobs:
  build-android:
    runs-on: windows-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Setup Java JDK
      uses: actions/setup-java@v4
      with:
        distribution: 'microsoft'
        java-version: '17'

    - name: Download fonts
      run: bash download-fonts.sh

    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: '8.0.x'

    - name: Install MAUI workload
      run: dotnet workload install maui

    - name: Restore dependencies
      run: dotnet restore UpRevFileOpener.Maui/UpRevFileOpener.Maui.csproj

    - name: Build Android app
      run: dotnet build UpRevFileOpener.Maui/UpRevFileOpener.Maui.csproj --configuration Release --framework net8.0-android --no-restore

    - name: Upload Android build artifacts
      uses: actions/upload-artifact@v4
      with:
        name: android-standalone-build
        path: UpRevFileOpener.Maui/bin/Release/net8.0-android/**/*
        retention-days: 30
